{
  "name": "taxnexus-nexus-alert-toolbar",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "TaxNexus Nexus Alert Toolbar \u2014 MV3 Chrome extension that flags CA nexus exposure on Amazon Seller Central.",
  "scripts": {
    "build": "vite build",
    "dev": "vite build --watch",
    "icons": "node scripts/generate-icons.mjs",
    "test": "node --test",
    "test:e2e": "../node_modules/.bin/playwright test --config playwright.config.mjs"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.2.4",
    "svelte": "^5.55.4",
    "vite": "^7.3.2"
  }
}
