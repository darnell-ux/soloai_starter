/** Public marketing name — safe to expose in HTML metadata */
export const SITE_BRAND = 'SaaS';

/** Recommended dimensions for primary social sharing assets (Open Graph) */
export const SOCIAL_IMAGE_WIDTH = 1200;
export const SOCIAL_IMAGE_HEIGHT = 630;

/** Fallback when page/CMS description is absent (callers should prefer localized copy) */
export const DEFAULT_META_DESCRIPTION =
	'Product and marketing pages for SaaS — secure, fast, and easy to use.';
